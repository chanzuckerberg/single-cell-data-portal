export const MAX_EXPRESSION_LABEL_TEST_ID = "max-expression-label";
export const COLOR_LEGEND_TOOLTIP_CONTENT =
  "After filtering cells with low coverage (less than 500 genes expressed), gene counts are normalized independently for each cell using ln(TTPM+1). The algorithm is summarized in detail in our  ";
export const COLOR_LEGEND = "Gene Expression color legend";
