import styled from "@emotion/styled";
import { Icon as SDSIcon } from "@czi-sds/components";
import { spacesXxs } from "src/common/theme";

export const Icon = styled(SDSIcon)`
  margin: ${spacesXxs}px 0;
`;
