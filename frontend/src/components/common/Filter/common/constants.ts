// App dependencies
import { CATEGORY_KEY } from "src/components/common/Filter/common/entities";

/* Configuration for displaying categories and their category values. */
export const CATEGORY_VIEW_CONFIG = {
  [CATEGORY_KEY.ASSAY]: {
    cate,
  },
};
