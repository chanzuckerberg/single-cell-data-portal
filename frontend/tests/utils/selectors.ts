export const getTestID = (id: string): string => `css=[data-testid="${id}"]`;

export const getText = (text: string): string => `text=${text}`;

export const getID = (id: string): string => `[id="${id}"]`;
